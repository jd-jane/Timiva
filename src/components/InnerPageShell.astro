---
interface Props {
	class?: string;
}

const { class: className } = Astro.props;
---

<div
	class:list={[
		"mx-auto mb-0 flex max-w-5xl flex-col gap-12 px-6 pb-20 pt-10 md:gap-16 md:pt-12",
		className,
	]}
>
	<slot />
</div>
